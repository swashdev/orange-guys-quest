*~
*.swp

# Ignore compiled python files to preserve source code purity
# (even though these compiled files will be generated by normal use, we
# shouldn't include them because they'll just end up being replaced by the new
# source code anyway)
*.pyc
